[pytest]
markers =
    curated_data: checks the hand-curated data files cover every accelerator
        version present in the scraped wheel matrix. These drift whenever
        NVIDIA/AMD ship a new toolkit, so they run in their own CI job and
        must not block the daily data refresh and Pages deploy.
